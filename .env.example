# ============================================================
# Email Outreach System — Environment Variables
# ============================================================
# Copy this file to .env and fill in your real values.
# NEVER commit .env to version control.
# ============================================================

# SMTP password for sending emails.
# Set this before running main.py or server.py.
# Usage: export SMTP_PASSWORD='your_password_here'
SMTP_PASSWORD=your_smtp_password_here

# Dashboard admin credentials (used in server.py for HTTP Basic Auth).
# Set a strong unique password before deploying.
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=your_dashboard_password_here

# Optional: override the default tracking domain if self-hosting
TRACKING_DOMAIN=https://your-tracking-domain.com
